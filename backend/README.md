# Backend

```bash
npm install
cp .env.example .env
npm run db:generate
npm run db:migrate
npm run dev
```

Core: shorten URLs + redirect. Redis optional.

See root `README.md` for full setup.
